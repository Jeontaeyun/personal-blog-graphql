test('create user', () => {
	expect(1).toBe(1);
});

test('create user Error with exuser', () => {
	expect(1).toBe(1);
});
test('login Error with No user', () => {
	expect(2).toBe(2);
});

test('login Error with wrong pasword', () => {
	expect(2).toBe(2);
});

test('create category', () => {
	expect(2).toBe(2);
});

test('create category Error with excategory', () => {
	expect(2).toBe(2);
});

test('create post', () => {
	expect(2).toBe(2);
});

test('read post', () => {
	expect(2).toBe(2);
});

test('read post Error with no post', () => {
	expect(2).toBe(2);
});

test('read posts', () => {
	expect(2).toBe(2);
});

test('update post', () => {
	expect(2).toBe(2);
});

test('update post Error with no post', () => {
	expect(2).toBe(2);
});

test('delete post', () => {
	expect(2).toBe(2);
});
test('delete post Error with no post', () => {
	expect(2).toBe(2);
});

test('ceate like', () => {
	expect(2).toBe(2);
});

test('ceate like Error with no post', () => {
	expect(2).toBe(2);
});

test('delete like', () => {
	expect(2).toBe(2);
});

test('delete like Error with no post', () => {
	expect(2).toBe(2);
});
test('create comment', () => {
	expect(2).toBe(2);
});

test('create comment Error with no access', () => {
	expect(2).toBe(2);
});

test('create comment Error with no post', () => {
	expect(2).toBe(2);
});
test('update comment', () => {
	expect(2).toBe(2);
});
test('update comment Error with no access', () => {
	expect(2).toBe(2);
});

test('update comment Error with no post', () => {
	expect(2).toBe(2);
});
test('delete comment', () => {
	expect(2).toBe(2);
});
test('delete comment Error with no access', () => {
	expect(2).toBe(2);
});

test('delete comment Error with no post', () => {
	expect(2).toBe(2);
});
test('logout', () => {
	expect(2).toBe(2);
});
