overwrite: true
schema: "./src/graphqls/schemas/schema.graphql"
documents: null
generates:
    src/types/graphql/graphql.ts:
        config:
            contextType: graphqls/context#IContext
        plugins:
            - "typescript"
            - "typescript-resolvers"
